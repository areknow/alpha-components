export const INSTALL_CODE = `$ npm install --save @miniml/alpha-components-react 
$ yarn add @miniml/alpha-components-react`;

export const FONT_CODE = `@import url('https://indestructibletype.com/fonts/Jost.css');`;

export const USAGE_CODE = `import { Root } from '@miniml/alpha-components-react/root';
import { Button } from '@miniml/alpha-components-react/button';

function App() {
  return (
    <Root>
      <Button type="primary">I think we click</Button>
    </Root>
  );
}`;
